-- Авто, удалённые/отключённые в Yume CRM, пропадают из ответа синка инвентаря,
-- но локальная запись остаётся (на неё ссылаются бронирования). Помечаем такие
-- авто флагом и скрываем из каталога, вместо того чтобы удалять.
ALTER TABLE "cars" ADD COLUMN "isArchived" BOOLEAN NOT NULL DEFAULT false;
