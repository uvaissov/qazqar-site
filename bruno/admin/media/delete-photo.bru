meta {
  name: Admin - Delete photo
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/api/admin/photos/:id
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

params:path {
  id: PHOTO_ID_HERE
}
