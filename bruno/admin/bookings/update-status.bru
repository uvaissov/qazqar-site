meta {
  name: Admin - Update booking status
  type: http
  seq: 2
}

patch {
  url: {{baseUrl}}/api/admin/bookings/{{bookingId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "status": "CONFIRMED"
  }
}
