meta {
  name: Login (OTP)
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/auth/login-otp
  body: json
  auth: none
}

body:json {
  {
    "email": "user@example.com",
    "code": "123456"
  }
}
